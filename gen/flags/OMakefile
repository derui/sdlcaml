################################################
# Configuration.  You may want to modify any of these configuration
# variables.
#
open MyUtil

.PHONY: gen
#
# This project requires ocamlfind (default - false).
#
USE_OCAMLFIND = true
#
OCAMLPACKS[] =
#
if $(not $(OCAMLFIND_EXISTS))
   eprintln(This project requires ocamlfind, but is was not found.)
   eprintln(You need to install ocamlfind and run "omake --configure".)
   exit 1

#
# Include path
#
# OCAMLINCLUDES += ../lib

#
# Compile native or byte code?
#
# The default values are defined as follows:
#
NATIVE_ENABLED = $(OCAMLOPT_EXISTS)
BYTE_ENABLED = true

#
# Various options
#
OCAMLFLAGS    += -bin-annot
# OCAMLCFLAGS   +=
# OCAMLOPTFLAGS += -linkpkg
# OCAML_LINK_FLAGS +=
# OCAML_BYTE_LINK_FLAGS +=
# OCAML_NATIVE_LINK_FLAGS +=

################################################
# Build bindings for OCaml to C.
#
#
# OCAML_LIBS += ../lib/mylib
# OCAML_CLIBS += outputflag
# OCAML_OTHER_LIBS += nums Glut GL bigarray vertArray VBO
# OCAML_LIB_FLAGS +=
# OCAML_PACKAGE_NAME = sdl_flags
#
FILES[] =
	sdl_window_event_id
	sdl_gl_context_flags
	sdl_gl_profile
	sdl_gl_attr
	sdl_blendmode
	sdl_pixel_format_enum
	sdl_syswm_type
	sdl_messagebox_flags
	sdl_renderer_flags
	sdl_renderer_flip
	sdl_texture_access
	sdl_texture_modulate
	sdl_init_flags
	sdl_event_type
	sdl_hat
	sdl_keymod
	sdl_mousebutton
	sdl_scancode
	sdl_keycode
	sdl_window_flags
	sdl_log_category
	sdl_log_priority
	sdl_game_controller_button
	sdl_availability
	sdl_controller_axis
	sdl_controller_device
	sdl_button_state
	sdl_dollar
	sdl_joy_device
	sdl_mousewheel
	sdl_finger
	sdl_state
	sdl_audio_device
	sdl_system_cursor
	sdl_audio_status
	sdl_audio_format
	sdl_audio_allow_status
	sdl_rwops_type
	sdl_controller_bind_type

MyOCamlPackage(sdlcaml_flags, $(FILES), $(EMPTY), $(EMPTY), false)
gen: $(OCamlPackageGeneratedFiles sdlcaml_flags, false)

.DEFAULT: gen
